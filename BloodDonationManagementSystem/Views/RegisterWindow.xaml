<Window x:Class="BloodDonationManagementSystem.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Account"
        Height="560"
        Width="780"
        MinHeight="560"
        MinWidth="720"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFF4F6F9">

    <Window.Resources>
        <LinearGradientBrush x:Key="MainBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFF7F9FC" Offset="0"/>
            <GradientStop Color="#FFFFE9EE" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FFCD2E4B"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#FFB7243F"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#FF1F2A44"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#FF7B8191"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#FFD7DCE6"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#FFF9FAFC"/>

        <Style x:Key="InputBoxStyle" TargetType="Control">
            <Setter Property="Height" Value="46"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="14,0,14,0"/>
            <Setter Property="Foreground" Value="#FF202738"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource MainBackgroundBrush}">
        <Border Width="420"
                Padding="34"
                Background="{StaticResource CardBrush}"
                CornerRadius="18"
                BorderBrush="#FFE5EAF2"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Create Account"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="{StaticResource TitleBrush}"/>
                <TextBlock Text="Join and start supporting blood donation today"
                           Margin="0,8,0,26"
                           FontSize="14"
                           Foreground="{StaticResource MutedBrush}"/>

                <Grid>
                    <TextBox x:Name="UsernameBox" Style="{StaticResource InputBoxStyle}"/>
                    <TextBlock Text="Username"
                               Margin="14,0,0,14"
                               VerticalAlignment="Center"
                               Foreground="#FF9CA3B3"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Text, ElementName=UsernameBox}" Value=""/>
                                            <Condition Binding="{Binding IsKeyboardFocused, ElementName=UsernameBox}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <Grid>
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource InputBoxStyle}"
                                 PasswordChanged="PasswordBox_OnPasswordChanged"
                                 GotFocus="PasswordBox_OnFocusChanged"
                                 LostFocus="PasswordBox_OnFocusChanged"/>
                    <TextBlock x:Name="PasswordPlaceholder"
                               Text="Password"
                               Margin="14,0,0,14"
                               VerticalAlignment="Center"
                               Foreground="#FF9CA3B3"
                               IsHitTestVisible="False"
                               Visibility="Visible"/>
                </Grid>

                <Grid>
                    <ComboBox x:Name="RoleBox"
                              Style="{StaticResource InputBoxStyle}"
                              SelectionChanged="RoleBox_OnSelectionChanged">
                        <ComboBoxItem Content="Donor"/>
                        <ComboBoxItem Content="Hospital"/>
                    </ComboBox>
                    <TextBlock x:Name="RolePlaceholder"
                               Text="Select role"
                               Margin="14,0,0,14"
                               VerticalAlignment="Center"
                               Foreground="#FF9CA3B3"
                               IsHitTestVisible="False"
                               Visibility="Visible"/>
                </Grid>

                <Button Content="Register"
                        Click="Register_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,8,0,16"/>

                <Button Content="Already have an account? Login"
                        Click="OpenLogin_Click"
                        Style="{StaticResource LinkButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>

</Window>
